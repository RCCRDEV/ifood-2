<Window x:Class="FoodDelivery.Views.Windows.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FoodDelivery" Height="720" Width="1180"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="270" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,1,0">
            <Grid Margin="18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Border Width="38" Height="38" CornerRadius="12" Background="{StaticResource BrushAccentGradient}" Margin="0,0,10,0">
                            <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#FF07100E"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="FoodDelivery" FontSize="{StaticResource FontSizeH1}" FontWeight="SemiBold" />
                            <TextBlock Foreground="{StaticResource BrushTextMuted}" FontSize="{StaticResource FontSizeSmall}" Text="Sistema de delivery" />
                        </StackPanel>
                    </StackPanel>

                    <Border Margin="0,16,0,0" Background="{StaticResource BrushSurface2}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="{StaticResource RadiusMd}" Padding="12">
                        <StackPanel>
                            <TextBlock Foreground="{StaticResource BrushTextMuted}" FontSize="{StaticResource FontSizeSmall}" Text="Conectado como" />
                            <TextBlock x:Name="UserTitleTextBlock" Margin="0,6,0,0" FontWeight="SemiBold" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <ListBox Grid.Row="2" Margin="0,16,0,0"
                         x:Name="NavListBox"
                         Style="{StaticResource NavListBox}"
                         ItemContainerStyle="{StaticResource NavListBoxItem}"
                         SelectionChanged="NavListBox_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding IconGlyph}" FontFamily="Segoe MDL2 Assets" FontSize="16" Width="24" Foreground="{StaticResource BrushText}" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Label}" Margin="10,0,0,0" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button Grid.Row="3" Content="Sair" Style="{StaticResource DangerButton}" Click="Logout_Click" />
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid Margin="18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="{StaticResource RadiusMd}" Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="PageTitleTextBlock" FontSize="{StaticResource FontSizeH1}" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <Border Grid.Column="1" Background="{StaticResource BrushSurface2}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="{StaticResource RadiusSm}" Padding="10,8" Margin="10,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource BrushTextMuted}" Margin="0,0,8,0" />
                                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Title}" Foreground="{StaticResource BrushTextMuted}" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Margin="0,14,0,0" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="{StaticResource RadiusMd}">
                    <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" />
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
