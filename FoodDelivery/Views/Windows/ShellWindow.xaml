<Window x:Class="FoodDelivery.Views.Windows.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FoodDelivery" Height="720" Width="1180"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="270" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource BrushSurface}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,1,0">
            <Grid Margin="18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="FoodDelivery" FontSize="20" FontWeight="SemiBold" />
                <TextBlock Grid.Row="1" Margin="0,10,0,0" Foreground="{StaticResource BrushTextMuted}" x:Name="UserTitleTextBlock"/>

                <ScrollViewer Grid.Row="2" Margin="0,20,0,0" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="NavStack" />
                </ScrollViewer>

                <Button Grid.Row="3" Content="Sair" Style="{StaticResource DangerButton}" Click="Logout_Click" />
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" />
        </Grid>
    </Grid>
</Window>

