<Page x:Class="FoodDelivery.Views.Pages.Cliente.RestaurantesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Restaurantes">
    <Grid Margin="{StaticResource ThicknessPage}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource Card}" Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Restaurantes" FontSize="18" FontWeight="SemiBold"/>
                <DataGrid Grid.Row="1" Margin="0,14,0,0"
                          x:Name="RestaurantesGrid"
                          SelectionMode="Single"
                          SelectionChanged="RestaurantesGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*" />
                        <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Style="{StaticResource Card}" Grid.Column="1" Margin="18,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Cardápio" FontSize="18" FontWeight="SemiBold"/>
                <TextBlock x:Name="SelectedRestauranteText" Grid.Row="1" Margin="0,8,0,0" Foreground="{StaticResource BrushTextMuted}"/>

                <DataGrid Grid.Row="2" Margin="0,14,0,0" x:Name="ProdutosGrid" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Produto" Binding="{Binding Nome}" Width="*" />
                        <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="140" />
                        <DataGridTextColumn Header="Preço" Binding="{Binding Preco}" Width="140" />
                        <DataGridTemplateColumn Header="" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Adicionar" Style="{StaticResource PrimaryButton}" Click="AddToCart_Click" Tag="{Binding}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>

